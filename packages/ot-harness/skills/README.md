# ot-harness Skills

This directory holds curated skill fixtures used by `ot-harness` variants.

- `neutral/` is intentionally empty aside from documentation so base Codex runs do not receive benchmark-relevant guidance.
- `smoke/` contains a small fixture skill for validating skill injection and metadata capture.
