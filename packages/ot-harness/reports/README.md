# ot-harness Reports

Curated Markdown or JSON summaries may live here when they are useful to review.
Raw Harbor jobs, trajectories, logs, scratch files, and generated trial output belong under `tmp/harness/harbor/` or another configured output root outside `packages/ot-harness/`.
