# add-cliproxy-llm-routing

Route LLM-backed OneTool operations through explicit CLIProxyAPI or direct
generation backends using shared model shortcuts and `low`, `medium`, or `high`
effort controls. Keep embeddings independently configured and every `ground`
operation on native Gemini.

Depends on `add-proxied-harness-launcher`.
