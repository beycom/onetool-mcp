## 1. Normalization & Command Preparation

- [x] 1.1 **(D1)** In `_force_single_quotes` (`src/ot/executor/runner.py:94-118`), skip re-quoting any string token whose decoded value (`ast.literal_eval(tok.string)`) contains a control character: `if "\n" in val or "\r" in val or any(ord(c) < 0x20 for c in val): result.append(tok); continue`. Add a unit test: `_force_single_quotes` on a double-quoted string containing both an apostrophe and a newline (e.g. `'note(text="Here\'s the plan:\nstep 1")'`) must not corrupt the string into an unterminated literal.
- [x] 1.2 **(D1)** Wrap the `_normalize_code(stripped, validation.ast_tree)` call inside `prepare_command` (`src/ot/executor/runner.py:522-523`, "Step 7: Normalize") in try/except; on any exception, fall back to the pre-normalization `stripped` value (already valid, validated Python) instead of propagating. Add a unit test that forces `_normalize_code` to raise (e.g. monkeypatch) and asserts `prepare_command` still returns a usable `PreparedCommand` with no error.
- [x] 1.3 **(D1)** Wrap the `prepared = prepare_command(command)` call inside `run` (`src/ot/server.py:543`) in try/except so any unanticipated exception during preparation becomes a clean, actionable error result flowing through the D2 `ToolError` path (task 2.1) instead of escaping the tool handler uncaught. Add a regression test: `run(command='note(text="Here\'s the plan:\nstep 1")')` (or the equivalent direct-executor call) executes successfully end-to-end — this is the exact reproduction from the deep dive and is also embedded in `## Verification` below.
- [x] 1.4 **(D-a1)** In `prepare_command` (`src/ot/executor/runner.py`, immediately after `stripped_cmd = command.strip()` at `:465`, alongside the existing `!onetool`-prefix check), add: if `not stripped_cmd`, return `PreparedCommand(code="", original=command, error="Command is empty. Provide a Python expression or tool call, e.g. pack.tool(arg=value).")`. Add a unit test: `prepare_command("")` and `prepare_command("   \n  ")` both return a `PreparedCommand` with `error` set (not `success=True`/"Code executed successfully (no return value)").
- [x] 1.5 **(D-a3)** Verify (no separate code change expected — task 1.1's guard is the fix): `_normalize_code`'s comment-dropping and `\t`→literal-tab rewriting on a string containing a tab do not corrupt the value once control-char tokens are skipped by 1.1. Add/extend a unit test asserting a string containing a literal tab character round-trips correctly through `_normalize_code`.

## 2. Error Contract (isError / MCP compliance)

- [x] 2.1 **(D2)** In `src/ot/server.py`, add `from fastmcp.exceptions import ToolError`. Replace `return ToolResult(content=f"Error: {prepared.error}")` at `:546` with `raise ToolError(f"Error: {prepared.error}")`. Replace the unconditional final `return ToolResult(content=text)` (`:572`, after the `sanitize_output` call at `:569-571`) with: if `not result.success`, `raise ToolError(text)`; else `return ToolResult(content=text)`. Confirm this works against the installed FastMCP 3.3.1 (no version bump — `ToolError` → `isError:true` is core behavior, not a 3.4.0 feature).
- [x] 2.2 **(D2)** Add a unit test: a command that fails preparation (e.g. references an unknown pack) causes `run()` to raise `ToolError`, and the exception message contains the original actionable error text.
- [x] 2.3 **(D2)** Add a unit test: a command whose execution raises a runtime exception causes `run()` to raise `ToolError` with the error text intact.
- [x] 2.4 **(D2)** Add a unit test: a command that executes successfully causes `run()` to return a `ToolResult` (not raise), i.e. `isError:false` behavior is preserved for the success path.
- [x] 2.5 **(D6)** In `execute_python_code`'s exception handler (`src/ot/executor/runner.py:424-430`), include `type(e).__name__` in the wrapped message: `f"❗️Execution error at line {n}: {type(e).__name__}: {e}"` (and the no-line-number variant similarly). Thread the *original* exception's type name through to `CommandResult.error_type` in `execute_command`'s outer except clause (`:677-686` region) — e.g. attach the original type name as an attribute on the wrapper exception before raising in `execute_python_code`, and read it back in `execute_command`'s except clause instead of using `type(e).__name__` on the (by-then-wrapped) exception. Add a unit test: code that raises `KeyError('missing')` produces a `CommandResult` with `error_type == "KeyError"` (not `"ValueError"`) and a result message containing `"KeyError"`.
- [x] 2.6 **(D6)** Verify the fix satisfies the *existing* `serve-run-tool` "Runtime error context" scenario ("error SHALL include the exception type and message") — no new spec delta needed for the message-text half of this fix; confirm via the test added in 2.5.

## 3. Threading & Concurrency

- [x] 3.1 **(D3)** In `execute_command` (`src/ot/executor/runner.py:598`), delete the `use_thread_pool = bool(proxy.servers)` conditional and the `if use_thread_pool: ... else: ...` branch (`:635-642` region) that runs `execute_python_code` synchronously on the event-loop thread when no proxy servers are connected. Always dispatch via `asyncio.to_thread(execute_python_code, stripped, tool_functions=tool_namespace, validate=should_validate, default_format="json")`.
- [x] 3.2 **(D3)** Add an integration test: start a slow/blocking tool call in one `run()` invocation (or simulate via a monkeypatched slow tool) with no proxy servers connected, and confirm a concurrent `run()`/`ping` request completes without waiting for the first — this reproduces and closes the "freeze" facet.
- [x] 3.3 **(D3)** In `src/ot/proxy/manager.py`, add the same `running_loop is self._loop` guard `reconnect_sync` already has (see the guard pattern around `:836-837`) to `connect_additional_sync` (`:737`, `future = asyncio.run_coroutine_threadsafe(self.connect_additional(name, config), self._loop); return future.result(timeout=120)` at `:751-755`) and to `disconnect_server_sync` (`:782`, blocking call at `:807-811`): when invoked from code already running on the manager's own loop, schedule via `self._loop.create_task(...)` instead of blocking `.result()`.
- [x] 3.4 **(D3)** Add a unit/integration test: call `connect_additional_sync`/`disconnect_server_sync` from code executing on the proxy manager's own event loop (simulating the `ot_servers.enable`/`restart`-with-nothing-connected deadlock path) and confirm it returns promptly instead of hanging until the 120s/30s timeout.
- [x] 3.5 **(D3)** Add a bounded per-tool execution timeout around the `asyncio.to_thread(...)` dispatch added in 3.1 (e.g. `asyncio.wait_for(asyncio.to_thread(...), timeout=<N>)`); pick and document a timeout value (see design.md Open Questions — the `webfetch` 30s figure is a reasonable floor). On timeout, produce a `CommandResult(success=False, ...)` that flows through the D2 `ToolError` path. Add a unit test: a tool call exceeding the timeout raises `ToolError` with a timeout-indicating message rather than hanging.
- [x] 3.6 **(D-c1)** In `ProxyManager.call_tool_sync` (`src/ot/proxy/manager.py:296-303`), wrap `future.result(timeout=timeout + 5)` in `try`/`except concurrent.futures.TimeoutError`, calling `future.cancel()` before re-raising (matching the pattern already used in the `fire_and_forget` branch's error handling immediately above). Add a unit test asserting `future.cancel()` is invoked (e.g. via a mock/spy on the future) when the call times out.
- [x] 3.7 **(D15)** In `ProxyManager.list_tools` (`src/ot/proxy/manager.py:159-163`), take `_mutation_lock` to snapshot `_tools_by_server` (e.g. `dict(self._tools_by_server)`) before building the `items` list, then iterate the snapshot outside the lock. Add a unit/integration test that concurrently mutates `_tools_by_server` (simulating a background connect) while `list_tools(server=None)` iterates, and confirms no `RuntimeError: dictionary changed size during iteration` is raised.

## 4. Parameter Resolution

- [x] 4.1 **(D4)** In `resolve_kwargs` (`src/ot/executor/param_resolver.py:70` def, collision logic at `:106-124`), track which provided key has already claimed each resolved target parameter name. When a second provided key (exact or prefix match) would resolve to a target already claimed by a *different* provided key, raise a clear `ValueError`/`TypeError` naming both colliding keys and the shared target instead of silently overwriting. Preserve the existing "multiple prefix matches, first-in-signature-order wins" behavior for the case of a *single* provided key ambiguously prefix-matching multiple *different* target names (do not conflate the two cases — see design.md D4 decision).
- [x] 4.2 **(D4)** Add a unit test reproducing the exact collision: `resolve_kwargs({'query': 'real', 'q': 'typo'}, ['query', 'count'])` raises an ambiguity error (does not return `{'query': 'typo'}`).
- [x] 4.3 **(D4)** Add a unit test reproducing the order-dependence case: both `resolve_kwargs({'c': 'A', 'count': 'B'}, ['count'])` and `resolve_kwargs({'count': 'B', 'c': 'A'}, ['count'])` raise the same ambiguity error (result must not depend on dict/kwarg order).
- [x] 4.4 **(D4)** Add a unit test confirming the existing "Multiple prefix matches with first-wins" behavior is unchanged: a tool with parameters `query_info`, `query`, `quality` (in that order) called with only `q="test"` still resolves to `query_info="test"`.
- [x] 4.5 **(D4)** Add an end-to-end regression test: `search(query='x', q='y')` (or an equivalent real tool call with a `query`-shaped parameter) raises a clear ambiguity error through the full `run()` path rather than silently binding `query='y'`. (This is also embedded in `## Verification` below.)

## 5. Nested Execution / Namespace Isolation

- [x] 5.1 **(D5)** In `_nested_run` (`src/ot/executor/runner.py:368-383`), exec nested commands into a **child namespace** (`dict(namespace)`, a shallow copy) instead of the shared outer `namespace` dict directly.
- [x] 5.2 **(D5)** Snapshot the three magics (`__format__`, `__sanitize__`, `__force_context__` — see the `global` declaration region around `:270`) from the outer namespace before the nested `exec`, and restore them onto the outer namespace after the nested `exec` completes, so a nested command's magic settings cannot leak into the outer command's result read at `:400-407`.
- [x] 5.3 **(D5)** Add a unit test: outer code sets `__format__ = 'json_h'` and calls `__onetool("<code that sets __format__ = 'raw'>")`; assert the outer result is still serialized with `json_h`, not `raw`. Add a parallel test for `__sanitize__` and `__force_context__`.
- [x] 5.4 **(D5)** Add a unit test: a nested `__onetool(...)` command assigns to a variable name also used by the outer command; assert the outer command's variable value is unaffected after the nested call returns.
- [x] 5.5 **(D-a2)** Add a recursion-depth counter for nested `__onetool(...)` calls (module-level or threaded through `_nested_run`'s closure state), bounded at a small limit (e.g. 5); raise a clear `ValueError` ("nested __onetool call depth exceeded") when the limit is exceeded, instead of allowing an unbounded recursion to raise `RecursionError`. Add a unit test: a command that recursively calls `__onetool(...)` beyond the limit raises the clear depth-limit error, not `RecursionError`.
- [x] 5.6 **(D-a4)** In `_nested_run`, compute the nested code's own `line_offset` (via `wrap_code_for_exec` on the nested code, mirroring the outer flow at `:389`) and pass it to `_map_error_line` (`:293-315`) when mapping exceptions raised inside the nested exec, instead of reusing the outer command's `line_offset`. Add a unit test: an exception raised on a specific line of a nested `__onetool(...)` command's source is reported with the correct nested-source line number, not a line number computed against the outer command's offset.

## 6. Serialization Resilience

- [x] 6.1 **(D8)** In `serialize_result` (`src/ot/utils/format.py`), add `default=str` to every `json.dumps` call (`:44` json, `:49` json_h). Add a unit test: a result dict containing `{"generated_at": datetime.now(), "score": Decimal("1.5")}` serializes successfully (not reported as an execution error).
- [x] 6.2 **(D9)** Add `allow_nan=False` to the same `json.dumps` calls (`:44`, `:49`, and the fallback at `:64`); catch the resulting `ValueError` when NaN/Infinity is present and substitute a JSON-safe value (implementer's choice of `null` vs. a string sentinel — document the choice at the call site). Add a unit test: a result containing `float('nan')` and one containing `float('inf')` both serialize to valid, `json.loads`-parseable JSON with no bare `NaN`/`Infinity` token in the output.
- [x] 6.3 **(D10)** Ensure the top-level fallback branches (`format.py:45,50,55,60,65`, the `return str(result)` paths for non-dict/list top-level values) produce behavior consistent with the now-degrading nested case from 6.1/6.2, and that `CommandResult.format` reflects the format actually produced. Add a unit test: a top-level `set` (e.g. `{1, 2, 3}` returned directly, not nested in a dict) serializes successfully with a `format` value matching what was actually produced, consistent with the same type nested inside a dict.
- [x] 6.4 **(D-b1)** Switch the `yml`/`yml_h` branches (`format.py:54`, `:59`) from `yaml.dump` to `yaml.safe_dump`, adding a fallback representer (or pre-coercing unrepresentable values via the same `default=str`-style degrade used for JSON) so unsafe tags (`!!python/object`, `!!set`) are never emitted and `RepresenterError` is never raised for a value the JSON path would have degraded instead. Add a unit test: a result containing a `set` serialized with `__format__ = 'yml'` produces valid, tag-free YAML (no `!!python/object`/`!!set` markers), and does not raise.
- [x] 6.5 **(D7)** In `execute_command` (`src/ot/executor/runner.py:652`, the `result_size = len(text_result.encode("utf-8"))` measurement inside the `allow_deflect` block), scrub lone surrogates from `text_result` before it is returned/measured: `text_result = text_result.encode("utf-8", "replace").decode()` (or equivalent), applied before the value leaves the serialization step. If exact byte-accurate sizing for the deflection threshold is needed, measure with `errors="surrogatepass"` separately from the scrubbed value that is actually returned. Add a unit test: a result string containing a lone UTF-16 surrogate (e.g. `"\ud800"` embedded in a filename-shaped string, simulating `os.fsdecode`/`surrogateescape` output) serializes and returns successfully without raising `UnicodeEncodeError`.

## 7. Output Sanitization

- [x] 7.1 **(D11)** In `src/ot/utils/sanitize.py:27-30`, change the `__ot\b` alternative in `TRIGGER_PATTERN` to a shape-aware pattern requiring a call shape to follow: `__ot\b\s*[\w.]*\(` (optional whitespace, optional dotted identifier, then a literal `(`), keeping `re.IGNORECASE`. Leave `__onetool\b` and `mcp__onetool\w*` unchanged.
- [x] 7.2 **(D11)** Update `tests/unit/core/test_sanitize.py::TestSanitizeOutput::test_sanitizes_when_enabled` (`:227` region) — its fixture `"Result with __ot trigger"` has no call shape after `__ot` and, under the fix, is a correctly-unredacted false positive; change the fixture to a genuine call-shaped trigger (e.g. `"Result with __onetool trigger"` or `"Result with __ot.file.delete() trigger"`) so the test continues to exercise real trigger detection. Confirm all other existing `test_sanitize.py` fixtures (`:24`, `:31`, `:38`, `:44`, `:53`, `:146`) still pass unchanged (they all use call-shaped triggers already).
- [x] 7.3 **(D11)** Add a unit test: `sanitize_triggers("see __ot for details")` returns the string unchanged (no `[REDACTED:trigger]`). Add a unit test: `sanitize_triggers("the __OT flag")` returns the string unchanged.
- [x] 7.4 **(D11)** Add a unit test: `sanitize_triggers("__ot.file.delete(path='x')")` (dotted, no space) still redacts `__ot`.
- [x] 7.5 **(D-b2)** Set `should_sanitize=False` on error `CommandResult`s built in `execute_command`'s except clause (`src/ot/executor/runner.py:680-686` region — currently relies on the dataclass default `should_sanitize=True`). Route the `prepare_command`-error path (now raising via D2's `ToolError` at `server.py:546`, task 2.1) through equivalent `should_sanitize=False` treatment so both first-party error surfaces are handled consistently (neither boundary-wrapped nor trigger-redacted). Add a unit test asserting both error paths produce the same sanitization treatment for equivalent error text.
- [x] 7.6 **(D-b3)** In the deflection block (`src/ot/executor/runner.py:656-662`), sanitize `ctx_content` before calling `result_store.store(ctx_content, ...)` (or sanitize at `ctx.read` time — document which approach is chosen and why). Add a unit test: a large output containing a trigger-pattern-shaped string is deflected, then read back via `ctx.read(handle=...)`; assert the trigger is redacted in the read-back content (not present in raw form).
- [x] 7.7 **(D-b3)** In the same deflection block, serialize `ctx_content` using the caller's requested `result_fmt` (already available in scope from `execute_python_code`'s return tuple) instead of unconditionally `json.dumps(raw_result, indent=2, ...)`. Add a unit test: code sets `__format__ = 'yml'` and produces output large enough to be deflected; assert the content later read via `ctx.read(handle=...)` is YAML-formatted, not JSON.

## 8. Proxy Result Conversion & Cache Hygiene

- [x] 8.1 **(D12)** In `ProxyManager.call_tool`'s result-extraction loop (`src/ot/proxy/manager.py:222-231`), add a branch handling `types.EmbeddedResource` content blocks: extract `.resource.text` (or a binary marker if the resource is not text) as a text part. Add a unit test: a mocked proxy result whose `content` contains an `EmbeddedResource` block surfaces its text, not `"Tool returned empty response."`.
- [x] 8.2 **(D12)** When the `content` loop yields no text parts, fall back to `result.structured_content`/`result.data` (whichever the installed FastMCP `CallToolResult` type exposes) before reporting an empty response. Add a unit test: a mocked proxy result with empty `content` but populated `structured_content` surfaces that structured payload.
- [x] 8.3 **(D13)** In the same method (`:232-237` region), restrict the JSON-coercion attempt to text that, stripped, starts with `{` or `[`; leave all other text unparsed. Add a unit test: a mocked proxy result with text `"007"` returns the string `"007"` unchanged (not the int `7`). Add a unit test: a mocked proxy result with text `"[1,2]"` still parses to the list `[1, 2]`.
- [x] 8.4 **(D14)** In `src/ot/executor/pack_proxy.py`, restructure `McpProxyPack` instance management (currently one instance implicitly reused via the outer `_namespace_cache` at `:257-304`, each with its own `_function_cache` at `:120-186`) so that `ProxyManager.connect_additional`, `disconnect_server`, and `reconnect` (`src/ot/proxy/manager.py:710`, `:757`, `:699` respectively) can evict/rebuild the specific server's `McpProxyPack` (and its `_function_cache`) even when the outer namespace-cache key (`frozenset(proxy_mgr.servers)`) is unchanged before/after — i.e. the restart case (disconnect immediately followed by reconnect of the same server name). Reuse or extend the existing `pack_proxy.reset()` (`:364-369`, currently only called by `ot.reload()`) as appropriate, or add a narrower per-server eviction hook.
- [x] 8.5 **(D14)** In `src/ot/executor/param_resolver.py`, evict `_mcp_param_cache` (`:33-67` region) entries whose key's `server_name` matches the affected server inside `ProxyManager.connect_additional`/`disconnect_server`/`reconnect` (on disconnect/restart; connect needs no eviction since a newly connected server has no stale entries).
- [x] 8.6 **(D14)** Add an integration test: connect a mock proxy server, call a tool through it to populate both caches (`McpProxyPack._function_cache` and `_mcp_param_cache`), then simulate a `restart` (disconnect + reconnect the same server name) where the mock server's tool schema has changed; assert a subsequent call resolves against the *new* schema, not the cached pre-restart one.

## 9. MCP / FastMCP Compliance

- [x] 9.1 **(F3)** Add a unit test asserting the registered `run` tool's `output_schema is None` (introspect the FastMCP tool registration via whatever accessor the test suite already uses for tool introspection). Add a comment at the assertion site explaining why: changing `run`'s `-> ToolResult` return annotation to `-> str`/`-> dict` would make FastMCP auto-generate an output schema, and because `run` always returns `structured_content=None`, that would make every call fail output-schema validation and return `isError:true` unconditionally.
- [x] 9.2 **(F4)** In `src/ot/server.py:165-171`, add `log_config=None` to the `uvicorn.Config(...)` call for the Direct API. Verify loguru's `InterceptHandler` still covers uvicorn's loggers after this change (check existing logging setup); if it does not, provide an explicit stderr-only/InterceptHandler-based `log_config` instead of `None`. No new test required (defensive config change at the current `warning` log level); note the verification performed in the PR description.
- [x] 9.3 **(Annotations)** In `src/ot/server.py:531-536`, change `"destructiveHint": False` to `"destructiveHint": True` in the `run` tool's `@mcp.tool(annotations=...)`. Add a unit test asserting the registered `run` tool's `destructiveHint` annotation is `True`.

## 10. Performance (R8 P1–P4)

- [x] 10.1 **(R8 P1 — same fix as D3)** Confirm task 3.1 (always `asyncio.to_thread`) fully addresses R8 P1 ("Tool code runs on the event loop when no proxy servers are configured"); no separate code change needed beyond 3.1–3.2.
- [x] 10.2 **(R8 P2)** In `packages/onetool-pack/src/otpack/cache.py:167`, change `cache = Cache(max_size=0)` to a bounded `max_size` (pick and document a generous value — see design.md Open Questions). Confirm `Cache`'s existing LRU eviction engages once the bound is reached (read `Cache`'s implementation to confirm this before assuming it). Add/extend a unit test: after exceeding `max_size` distinct memoized keys, the oldest entries are evicted (cache size never exceeds the bound).
- [x] 10.3 **(R8 P2)** Run the full test suites for every consumer of the shared `cache` singleton after bounding it: `webfetch` (`src/otdev/tools/webfetch.py:130`, `_fetch_url_cached`), `context7`, `ot_forge`, `skills`. Confirm no regressions from the new bound at normal usage volumes.
- [ ] 10.4 **(R8 P3, optional)** In `execute_command` (`src/ot/executor/runner.py:653-664`), avoid the third full serialization pass: reuse the already-computed `text_result` string length instead of re-encoding to measure size, and avoid re-`json.dumps`-ing `raw_result` when `text_result` is already the correct JSON serialization of it. If implemented, add a unit test confirming deflection still produces correct stored content and size accounting.
- [ ] 10.5 **(R8 P4, optional)** In `src/ot/registry/registry.py`, thread the `ast.Module` parsed once (at `:182-183` region, the tool-extraction pass) through to `_extract_pack_metadata` (`:295-298` region, which currently re-`read_text`+`ast.parse`s the same file) instead of parsing each tool file's AST twice at startup. If implemented, add a unit test confirming registry startup still extracts the same pack/tool metadata as before.

## 11. Spec Sync Verification

- [x] 11.1 Run `openspec status --change "p12-core-flow-hardening"` and confirm proposal, design, specs, and tasks all show complete/parseable.
- [x] 11.2 Confirm every `#### Scenario:` header in the delta specs (`specs/serve-run-tool/spec.md`, `specs/serve-mcp-proxy/spec.md`, `specs/serve-server-management/spec.md`, `specs/serve-output-sanitization/spec.md`, `specs/serve-stats/spec.md`) uses exactly four hashtags.
- [x] 11.3 Confirm each MODIFIED requirement block was copied from the corresponding `openspec/specs/<capability>/spec.md` file in full before editing (no partial-block content loss) — diff each MODIFIED block against the current main spec to confirm only the intended scenarios were added/changed.

## Verification

Acceptance checks (embedded verbatim from the source deep dive's Acceptance Checks section — every command below MUST be run, and MUST pass, before this change is considered complete):

- [x] V.1 `note(text="Here's the plan:\nstep 1")` and other apostrophe+newline strings execute successfully (regression test for D1); `prepare_command` never raises out of `run`.
- [x] V.2 A failing `run` (validation error, user exception) yields `isError:true` with the error text intact; a success yields `isError:false` (unit test on both).
- [x] V.3 A blocking tool no longer stalls a concurrent `run`/`ping`; `ot_servers.enable`/`restart` with nothing connected returns promptly, not after a 120s timeout (D3 tests).
- [x] V.4 `search(query='x', q='y')` raises an ambiguity error rather than silently binding `query='y'` (D4).
- [x] V.5 Tools returning `{"t": datetime.now()}`, `float('nan')`, a `set`, or a filesystem name with invalid UTF-8 return a valid result, not an "execution error" or codec crash (D7–D10).
- [x] V.6 A proxied tool returning an `EmbeddedResource` or structured-only result surfaces its payload, not "empty response" (D12); `"007"` stays `"007"` (D13).
- [x] V.7 Returned content containing `__ot`/`__onetool` in prose is not redacted (D11).
- [x] V.8 Changing `run`'s return annotation is caught by a test asserting `output_schema is None` (F3).

Additional checks derived for this change:

- [x] V.9 `uv run pytest -m unit` passes with all new/updated tests from sections 1–10 included.
- [x] V.10 `uv run pytest -m integration` passes (covers the D3 non-blocking and D14 restart-cache-eviction integration tests).
- [x] V.11 `rg "use_thread_pool" src/ot/executor/runner.py` returns empty (the conditional was deleted, not merely disabled, per task 3.1).
- [x] V.12 `rg "yaml\.dump\(" src/ot/utils/format.py` returns empty (confirms `yaml.safe_dump` fully replaced unsafe `yaml.dump` per task 6.4).
- [x] V.13 `rg "ToolResult\(content=f\"Error: \{prepared.error\}\"\)" src/ot/server.py` returns empty (confirms the D2 `prepared.error` path now raises `ToolError` instead of returning, per task 2.1).
- [x] V.14 `just check` passes (lint via ruff, strict mypy, full pytest suite).
- [x] V.15 `openspec status --change "p12-core-flow-hardening"` shows all four artifacts complete.
