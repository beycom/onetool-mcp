## 1. `onetool init` idempotency and `--force` (repeatable-install-and-config-flow.md Expected: "the canonical project initialization command", "whether initialization is idempotent by default", "behavior when config files already exist in interactive and non-interactive shells")

- [x] 1.1 Add a `--force` boolean option to `init_callback` in `src/onetool/cli.py:382-390` (alongside the existing `--config`/`-c` option).
- [x] 1.2 In the non-interactive branch (`src/onetool/cli.py:477-480`): before calling `_write_onetool_yaml`, check `config_path.exists()`. If it exists and `--force` was not passed, print a message stating the config already exists and that `--force` overwrites it, then `raise typer.Exit(0)` without touching any file. If `--force` was passed or the file does not exist, proceed exactly as today (write via `_write_onetool_yaml`, which already backs up via `_safe_write`).
- [x] 1.3 In the interactive/TTY branch (`src/onetool/cli.py:431-437`, the `questionary.confirm("onetool.yaml already exists. Overwrite?", ...)` block): when `--force` is passed, skip the confirmation prompt entirely and proceed directly to the extension checkbox TUI (print a short note that `--force` is overwriting).
- [x] 1.4 Update the `init_callback` docstring (`src/onetool/cli.py:392-401`) to document the new idempotent-by-default / `--force` contract, so `onetool init --help` states the same behavior the docs describe.
- [x] 1.5 After a successful write in both branches (end of TTY branch `src/onetool/cli.py:471-475`, end of non-interactive branch `src/onetool/cli.py:479-480`), print a "next step" hint recommending `onetool init validate --config <config_path>`.
- [x] 1.6 Unit tests in `tests/unit/core/test_init_redesign.py` (pattern: `@pytest.mark.unit @pytest.mark.core`, `CliRunner().invoke(app, [...])` from `tests/unit/core/test_cli_runtime_modes.py`): (a) non-interactive re-run against an existing config is a no-op (file mtime/content unchanged, exit code 0, no `.bak` created); (b) non-interactive re-run with `--force` overwrites and creates a `.bak`; (c) the post-write output contains the `onetool init validate` hint text.

## 2. Startup failure when `--secrets` points at a missing file (repeatable-install-and-config-flow.md Expected: "the expected failure message when startup cannot find required config or secrets")

- [x] 2.1 Add an `explicit: bool = False` parameter to `get_secrets` in `src/ot/config/secrets.py:148-171`; pass it through to the existing `load_secrets(secrets_path, explicit=explicit)` call at `secrets.py:170` (the `explicit=True` → `FileNotFoundError` branch already exists in `load_secrets` at `secrets.py:71-75` — do not duplicate that logic).
- [x] 2.2 In `load_config` (`src/ot/config/loader.py:392-459`), change the call at `loader.py:455` from `get_secrets(secrets_path, reload=True)` to `get_secrets(secrets_path, reload=True, explicit=secrets_path is not None)`.
- [x] 2.3 Verify (no code change expected) that `_load_runtime_config` in `src/onetool/cli.py:105-114` — used by both `serve_command` and `root_callback` — already surfaces any exception from `get_config(...)` via `_write_startup_config_error` + `console.print(f"[red]Error loading config:[/red] {e}")` + `typer.Exit(1)`, and that a `FileNotFoundError("Secrets file not found: ...")` therefore produces a clear, actionable stderr message with no further changes needed. If the message is not sufficiently actionable (does not name the secrets path or does not distinguish itself from a config-loading error), add a `FileNotFoundError`-specific branch to `_load_runtime_config` that also prints a pointer to `onetool init`.
- [x] 2.4 In `onetool kb` and `onetool init validate` (`src/onetool/cli.py:632-633`, `load_secrets(secrets)` call inside `init_validate`), change to `load_secrets(secrets, explicit=secrets is not None)` so an explicit `--secrets` path that doesn't exist is reported as an error in the "Secrets" section rather than silently showing "(none configured)".
- [x] 2.5 Unit test in `tests/unit/core/test_cli_runtime_modes.py` (or a new adjacent test module, matching its `CliRunner().invoke(app, ["serve", "--config", ..., "--secrets", ...])` pattern): `onetool serve --config <existing config> --secrets <nonexistent path>` exits non-zero and stderr contains `"Secrets file not found"` and the nonexistent path. A second test confirms omitting `--secrets` entirely still starts normally with no error (regression guard for the "secrets remain optional" contract).
- [x] 2.6 Unit test for `onetool init validate --config <existing> --secrets <nonexistent>`: output includes an error naming the missing secrets path instead of "(none configured)".

## 3. `onetool init mcp-config` command (report R3 item 3: `onetool init mcp-config [--client claude-code|claude-desktop|cursor|vscode]` printing ready-to-paste JSON with resolved paths)

- [x] 3.1 Add `@init_app.command("mcp-config")` to `src/onetool/cli.py` (adjacent to `init_validate` at `cli.py:483`), with options `--client` (`Literal["claude-code","claude-desktop","cursor","vscode"] | None`, default `None`), `--config`/`-c` (same suffix-detection resolution as `init_callback`, default `onetool.yaml` in cwd), `--secrets`/`-s` (default `<config-dir>/secrets.yaml`).
- [x] 3.2 Resolve the `onetool` command path via `shutil.which("onetool")`; if `None`, fall back to the literal string `"onetool"` and print a stderr warning that it was not found on `PATH`.
- [x] 3.3 Resolve `--config` to an absolute path via `.resolve()`. If it does not exist on disk, still print the resolved path and print a stderr warning that `onetool init --config <path>` has not been run yet.
- [x] 3.4 Resolve `--secrets` (default `<config-dir>/secrets.yaml`) to an absolute path. If it does not exist on disk, omit `--secrets ...` from every printed `args` array and print a stderr note that `onetool init` creates it.
- [x] 3.5 Implement the `mcpServers`-keyed JSON block (used by `claude-code`, `claude-desktop`, `cursor`): `{"mcpServers": {"onetool": {"command": "<resolved onetool path>", "args": ["serve", "--config", "<resolved config path>", "--secrets", "<resolved secrets path>"]}}}` (secrets arg per 3.4).
- [x] 3.6 Implement the `servers`-keyed JSON block for `vscode` (distinct shape — VS Code uses `servers`, not `mcpServers`, and requires `"type": "stdio"` per entry): `{"servers": {"onetool": {"type": "stdio", "command": "<resolved onetool path>", "args": [...]}}}`.
- [x] 3.7 For `claude-code`, also print the `claude mcp add onetool -- <resolved onetool path> serve --config <resolved config path> --secrets <resolved secrets path>` one-liner and name `~/.claude/mcp.json` (or project `.mcp.json`) as the merge target.
- [x] 3.8 For `claude-desktop`, detect the current OS (`platform.system()`) and name the correct target file: `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS), `%APPDATA%\Claude\claude_desktop_config.json` (Windows), `~/.config/claude-desktop/claude_desktop_config.json` (Linux); print a note that the block must be merged into that file's existing `mcpServers` key.
- [x] 3.9 For `cursor`, name `.cursor/mcp.json` (relative to cwd) as the primary target and mention `~/.cursor/mcp.json` as the global alternative.
- [x] 3.10 For `vscode`, name `.vscode/mcp.json` (relative to cwd) as the primary target and mention the user-profile `mcp.json` as the global alternative.
- [x] 3.11 When `--client` is omitted, print all four clients in order (`claude-code`, `claude-desktop`, `cursor`, `vscode`), each under its own heading.
- [x] 3.12 Unit tests in a new `tests/unit/core/test_init_mcp_config.py` (pattern from `test_init_redesign.py`/`test_cli_runtime_modes.py`): one test per client asserting the correct top-level key (`mcpServers` vs `servers`), the `"type": "stdio"` field for vscode only, resolved absolute paths (no `/path/to/...` or `/Users/yourname/...` placeholders in output), `--secrets` omitted when the secrets file doesn't exist, and the no-`--client` case printing all four headings.

## 4. Bootstrap installer scripts (report R3 item 0: `curl … | sh` + PowerShell bootstrap wrapping uv)

- [x] 4.1 Write `scripts/install.sh` (POSIX sh, macOS/Linux): detect platform; check `uv` on `PATH`, install via `curl -LsSf https://astral.sh/uv/install.sh | sh` if missing and source its env hook so `uv` is usable in the rest of the script; run `uv tool install "onetool-mcp[${ONETOOL_EXTRAS:-all}]"`; run `onetool init --config "${ONETOOL_CONFIG_DIR:-$HOME/.onetool}"`; run `onetool init mcp-config --config "${ONETOOL_CONFIG_DIR:-$HOME/.onetool}/onetool.yaml"`; print a closing message pointing at `onetool init validate` and noting that `onetool init` ran non-interactively (re-run it in a terminal for extensions/secrets).
- [x] 4.2 Write `scripts/install.ps1` (PowerShell, Windows): same five steps as 4.1, using `irm https://astral.sh/uv/install.ps1 | iex` for the uv bootstrap and `$env:ONETOOL_EXTRAS`/`$env:ONETOOL_CONFIG_DIR` (default `$env:USERPROFILE\.onetool`) for overrides.
- [x] 4.3 Add a `just` recipe (e.g. `docs-install-scripts`) that copies `scripts/install.sh` → `docs/install.sh` and `scripts/install.ps1` → `docs/install.ps1`, and generates `docs/install.sh.sha256` / `docs/install.ps1.sha256` via `shasum -a 256`. Wire this recipe as a dependency of the existing `docs-build` and `docs-deploy` recipes (`justfile:127-136`) so the docs-domain copies can never go stale relative to `scripts/`.
- [x] 4.4 `bash -n scripts/install.sh` (syntax check) — run and confirm zero errors; record in the Verification section below.
- [ ] 4.5 Manual smoke test: run `scripts/install.sh` end to end on a machine/container with no prior `uv`/`onetool` install; confirm `uv` gets installed, `onetool` ends up on `PATH`, `onetool.yaml` is created, and `onetool init mcp-config` output is printed at the end with resolved (non-placeholder) paths.

## 5. Documentation: recommended install sequence and MCP client config

- [x] 5.1 `README.md:53-85` (Install section): make the bootstrap one-liner (`curl -LsSf https://onetool.beycom.online/install.sh | sh`) the primary documented command; keep `uv tool install 'onetool-mcp[all]'` + `onetool init` as the explicit manual alternative; replace the hard-coded placeholder JSON block (`README.md:70-79`) with instructions to run `onetool init mcp-config --client claude-code` (or the relevant client) and paste its output.
- [x] 5.2 `docs/learn/quickstart.md` steps 2/3/6: same treatment — bootstrap one-liner as the fast path; replace the placeholder JSON at `quickstart.md:82-91` with the `onetool init mcp-config` instruction; add `onetool init validate` (already step 5) as the explicit final check before "Connect to Claude Code".
- [x] 5.3 `docs/learn/installation.md:37-73` (Install section): document the bootstrap script alongside the existing manual `uv tool install` table; add the inspect-first + checksum verification sequence (`curl -LsSf <url> -o install.sh && shasum -a 256 -c install.sh.sha256 && sh install.sh`) per the `onetool-install-flow` "Installer distribution and integrity" spec requirement.
- [x] 5.4 `docs/learn/installation.md:146-167` (MCP Configuration section): replace the single hard-coded Claude Code JSON block with a new "Other clients" structure driven by `onetool init mcp-config`'s own output — one subsection per client (`claude-code`, `claude-desktop`, `cursor`, `vscode`), each showing the command to run and its output shape, closing the "only Claude Code is documented despite 'any MCP client'" gap (`README.md:20`).
- [x] 5.5 Do not edit the Python-version prerequisite lines (`installation.md:3,11,19,26,33`) or the extras table's package contents (`installation.md:49-53`) — those belong to `p18-docs-debt-sweep` and `p16-extras-restructure` respectively; only touch the Install and MCP Configuration sections listed above.

## Verification

- [x] `just check` passes (lint + typecheck + test).
- [x] `bash -n scripts/install.sh` — zero syntax errors.
- [x] `uv run pytest -m "unit and core" -k "init_redesign or init_mcp_config or cli_runtime_modes"` — all new and existing tests pass.
- [x] `rg -n "yourname|/path/to/\.onetool" README.md docs/learn/quickstart.md docs/learn/installation.md` — empty (no placeholder paths remain in the docs this change touches).
- [x] `rg -n "onetool init mcp-config" README.md docs/learn/quickstart.md docs/learn/installation.md` — non-empty (the new command is actually referenced where the placeholder JSON used to be).
- [ ] Manual clean-machine flow (see design.md's "Clean-machine flow" scenario and tasks 4.5): bootstrap install → `onetool init` → paste `onetool init mcp-config` output into an MCP client → `onetool init validate` → first `ot.status()` call succeeds, with no hand-edited paths anywhere in the sequence.
- [ ] Manual: `onetool init --config <path>` run twice with no `--force` — confirm the second run is a no-op (exit 0, no file changes, no `.bak` created) and prints the `--force` hint; run a third time with `--force` and confirm it overwrites with a `.bak` backup.
- [ ] Manual: `onetool init validate` is confirmed to be the command referenced as the final step in all three touched docs (`README.md`, `quickstart.md`, `installation.md`) and in `onetool init --help`/the post-init hint text — the same command in all four places.
- [ ] `diff scripts/install.sh docs/install.sh` and `diff scripts/install.ps1 docs/install.ps1` — empty (docs-domain copies match the versioned source; run after `just docs-install-scripts` or equivalent).
