## 1. Freeze the Data and Configuration Contracts

- [x] 1.1 Add strict typed models for worker-maintained context, committed context,
  internal terminal output, execution policy, and the three-field public result;
  test required fields, enums, blank values, unknown fields, and exact shapes.
- [x] 1.2 Add strict `tools.worker` configuration for optional `model`, `effort`,
  and positive-integer `context_max_kb=16`; test defaults, invalid values,
  unknown keys, and per-call model and effort precedence.
- [x] 1.3 Add deterministic normalization and canonical YAML rendering for the
  fixed context schema; test ordering, whitespace repair, exact deduplication,
  path normalization, UTF-8/LF output, and byte measurement.

## 2. Implement the Episodic Context Store

- [x] 2.1 Resolve `episodic-context/<session-id>/context.yaml` through existing
  project-state and project-path utilities; create opaque session IDs and test
  unknown, cross-project, absolute, escaping, missing, and non-file references.
- [x] 2.2 Implement safe startup preflight for complete context loading,
  normalization, strict validation, canonical rewrite, reference checks, and the
  KB limit; test that every invalid case returns `failed` before worker startup
  and preserves the existing file.
- [x] 2.3 Implement exact loaded-revision checks and beside-file atomic commits for
  optional terminal context; test increments, absent context, stale revisions,
  interrupted writes, failures, and last-valid-state preservation.

## 3. Implement the Codex App-Server Adapter

- [x] 3.1 Build the focused app-server adapter against schemas generated by the
  installed Codex version, including initialization, `thread/delete` capability
  preflight, one fresh thread, one turn, and strict worker terminal output.
- [x] 3.2 Validate and map explicit current-project `cwd`, `approval_policy: never`,
  disabled network, and read-only or workspace-write sandboxing; test failure
  before thread startup for invalid or unrepresentable policies.
- [x] 3.3 Normalize completion, required-input, protocol failure, timeout, malformed
  output, process exit, and caller interruption into the four specified statuses;
  test that every result contains only `session_id`, `status`, and `message`.
- [x] 3.4 Delete every finished worker thread after context handling; test normal
  deletion and cleanup failure warning behavior without changing the episode or
  context outcome and without resuming a thread.

## 4. Add the Small Worker Tool Surface

- [x] 4.1 Add sole tool `worker.run(prompt, execution, session_id, model, effort)`
  with new-session and explicit-session behavior, configured routing defaults,
  complete startup context, optional terminal context, and normalized public result.
- [x] 4.2 Enforce one active call, mark the child environment, reject recursive
  and concurrent calls, and never retry an episode automatically; verify these
  boundaries with the fake app-server harness.

## 5. Distribute the Explicit Orchestrator Skill

- [x] 5.1 Add `skills/episodic-orchestrator/SKILL.md` with the coordinator-only
  workflow: first-call session creation, same-session follow-ups, terminal relay,
  and `needs_input` handling without substantive main-agent project work.
- [x] 5.2 Add `skills/episodic-orchestrator/agents/openai.yaml` with implicit
  invocation disabled; test standard skill discovery, frontmatter, sidecar policy,
  and the absence of OneTool-owned skill installation or serving.

## 6. Verify the Complete V1 Slice

- [x] 6.1 Add an integration test covering two successful episodes in one session,
  fresh thread IDs, whole-context continuation, no prior worker messages, and
  deterministic commit or preservation for all four terminal statuses.
- [x] 6.2 Add contract tests proving v1 exposes only `worker.run`: no context save,
  read, search, selector, patch, compaction, queue, scheduler, retry,
  artifact-store, or compatibility surface.
- [x] 6.3 Document the worker tool calls, exact result statuses, context schema,
  16 KB default, configuration precedence, and explicit skill invocation without
  presenting deferred `next.md` ideas as implemented behavior.
- [x] 6.4 Run the focused worker and context tests, strict OpenSpec validation, and
  `just check`; resolve all failures before marking the change implemented.
