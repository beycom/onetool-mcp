# Tasks: otpack-embedding-infra

## 1. otpack shared module (pure addition)

- [x] 1.1 Create `packages/onetool-pack/src/otpack/embedding.py` with module constants `TOKEN_SAFETY_MARGIN = 100` and `MODEL_NATIVE_DIMS` (text-embedding-3-small: 1536, text-embedding-3-large: 3072, text-embedding-ada-002: 1536); no `ot.*`/`ottools`/`otutil`/`otdev` imports anywhere in the file
- [x] 1.2 Implement pure helpers: `get_tiktoken_encoding(model)` (lazy `import tiktoken` with `onetool-pack[embedding]` install hint, `cl100k_base` fallback on `KeyError`), `chunk_text_by_tokens(text, max_tokens, model)`, `dimensions_param(model, configured)` (return configured only when it differs from `MODEL_NATIVE_DIMS` native default, else None)
- [x] 1.3 Implement serialization: `serialize_embedding(vec)` → `struct.pack(f"<{n}f", *vec)` (None → None), `deserialize_embedding(blob)` (None → None, `ValueError` when `len(blob) % 4 != 0`), `cosine_similarity_blobs(a, b)` (None passthrough, `ValueError` on length mismatch, 0.0 on zero norm) — port body from `src/otutil/tools/_mem/db.py:_cosine_similarity`
- [x] 1.4 Implement `rrf_merge(list_a, list_b, limit, *, k=60, id_key="id", boost=None)` per design D5 (1-based ranks, list_a-first result precedence, additive boost callback, score rounded to 4 decimals written into each result)
- [x] 1.5 Implement `EmbeddingClient` (design D1): lazy OpenAI client (install-hint ImportError), `_prepare_safe_batch` (truncate to `max_tokens - safety_margin`, blank → `" "`), `_embed_batch_with_retry` (retry only HTTP 429/500/503 via `status_code` attr, backoff `2.0 ** attempt`, count-guard `ValueError`, retry warning logged as `f"{log_prefix}.embedding.retry"`), `embed(text, long_text="truncate"|"mean", use_cache=False)` with per-instance LRU cache keyed `(sha256(text), model, dimensions)` (TTL 900s, cap 256), `embed_batch(texts, batch_size=200, on_batch=None, max_attempts=3)`; LogSpans `f"{log_prefix}.embedding"` / `f"{log_prefix}.embedding.batch"`; pass `dimensions=` only when `dimensions_param` returns non-None
- [x] 1.6 Export all new public symbols from `packages/onetool-pack/src/otpack/__init__.py` (docstring + imports + `__all__`)
- [x] 1.7 Add `[project.optional-dependencies] embedding = ["openai>=2.44.0", "tiktoken>=0.12.0"]` to `packages/onetool-pack/pyproject.toml`
- [x] 1.8 Add `packages/onetool-pack/tests/test_embedding.py` (markers `unit`, `pkg`; mock OpenAI client): serialization round-trip + explicit-LE byte check (`== struct.pack("<1f", 1.0)`) + truncated-blob ValueError; cosine (identical/orthogonal/None/mismatch); chunking (fits → `[text]`, over-limit → lossless windows); `dimensions_param` (native → None, non-native → value, unknown model → None); `rrf_merge` (both-lists outranks, boost applied, limit respected); cache (hit skips API, eviction at cap, distinct key on model/dimensions); retry (429 then success succeeds, 400 no retry, count mismatch raises ValueError); `embed` strategies (truncate sends 1 input, mean sends N and returns element-wise average); missing-lib ImportError message includes `onetool-pack[embedding]`
- [x] 1.9 Verify boundary and package tests pass: `uv run pytest packages/onetool-pack/tests/test_boundary.py packages/onetool-pack/tests/test_embedding.py`

## 2. knowledge pack adoption

- [x] 2.1 Rewrite `src/otutil/tools/_knowledge/embedding.py` as thin adapter: keep `_get_config`/`_get_embedding_model`/secret lookup; add module-cached `_get_embedding_client()` keyed on `(api_key, model, base_url, dimensions, max_tokens)` building `EmbeddingClient(..., timeout=60.0, log_prefix="kb")`; `generate_embedding(text)` → `client.embed(text, use_cache=True)`; `generate_embeddings_batch(texts, batch_size, on_batch)` → `client.embed_batch(..., batch_size=batch_size or config.embedding_batch_size)`; delete `vec_to_bytes`, `_prepare_safe_batch`, `_embed_batch_with_retry`, `_dimensions_param`, `_EMBED_CACHE*`, `_MODEL_NATIVE_DIMS`, tiktoken helpers; update `__all__`
- [x] 2.2 Update `src/otutil/tools/_knowledge/db.py`: `serialize_embedding`/`deserialize_embedding` delegate to (or re-export) `otpack.serialize_embedding`/`otpack.deserialize_embedding`; drop local `struct` packing
- [x] 2.3 Update `vec_to_bytes` call sites to canonical serializer: `src/otutil/tools/_knowledge/indexer.py:332`, `src/otutil/tools/_knowledge/crud.py:318`, `src/otutil/tools/_knowledge/search.py:101` (import `serialize_embedding`)
- [x] 2.4 Update `src/otutil/tools/_knowledge/search.py`: `_rrf_merge` becomes a thin wrapper over `otpack.rrf_merge` passing `boost=lambda r: 0.1 * min(r.get("hit_count", 0) or 0, 10) / 10`
- [x] 2.5 Update `tests/otutil/unit/tools/test_knowledge.py` for moved/removed symbols (patch targets now on `otpack.embedding` client or the pack adapter); run `uv run pytest tests/otutil/unit/tools/test_knowledge.py`

## 3. mem pack adoption

- [x] 3.1 Rewrite `src/otutil/tools/_mem/embedding.py` generation core: keep queue/worker/enqueue machinery, `_get_config`, `_get_embedding_model`, secret lookup; add module-cached `_get_embedding_client()` building `EmbeddingClient(..., max_tokens=config.max_embedding_tokens, log_prefix="mem")`; `_generate_embedding(text)` → `client.embed(text, long_text="mean")`; delete local `_get_openai_client`, tiktoken helpers, `_chunk_text_by_tokens`, manual chunk-averaging; keep `_embed_now`/`_enqueue_after_commit` semantics unchanged; update `__all__` (keep a `_chunk_text_by_tokens` re-export from otpack if tests still reference it, else update tests)
- [x] 3.2 Add query-path caching: `src/otutil/tools/_mem/search.py` semantic/hybrid query embedding uses `client.embed(query, long_text="mean", use_cache=True)` (content writes/worker stay uncached)
- [x] 3.3 Update `src/otutil/tools/_mem/db.py`: `_serialize_embedding`/`_deserialize_embedding` delegate to otpack canonical helpers; `_cosine_similarity` UDF body delegates to `otpack.cosine_similarity_blobs` while preserving the existing dimension-mismatch message referencing `mem.reindex(dry_run=False)`
- [x] 3.4 Update `src/otutil/tools/_mem/search.py`: `_search_hybrid` fusion delegates to `otpack.rrf_merge` (k=60, no boost); result `score` semantics unchanged
- [x] 3.5 Update `tests/otutil/unit/tools/test_mem.py` and `tests/integration/tools/test_mem.py` for moved symbols/patch targets; add assertions: mem long-text embed averages windows (mean strategy), sync path retries a 429, repeated search query hits cache; run `uv run pytest tests/otutil/unit/tools/test_mem.py -m "unit"`

## 4. Verification and docs

- [x] 4.1 Grep-verify no non-canonical packing remains: `grep -rn 'struct.pack' src/otutil/tools/_mem src/otutil/tools/_knowledge` returns no `f"{n}f"`-native embedding packs and no duplicated `<{n}f` bodies outside the otpack delegates; `vec_to_bytes` no longer exists
- [x] 4.2 Byte-compat sanity check: existing-store scenario — build a knowledge DB with the pre-change native packer (or fixture blob), open with new code, assert semantic search still returns correct nearest neighbours (no reindex)
- [x] 4.3 Run full affected suites: `uv run pytest tests/otutil/unit/tools/test_mem.py tests/otutil/unit/tools/test_knowledge.py packages/onetool-pack/tests -m "unit or pkg"` and `uv run pytest tests/integration/tools/test_mem.py -m integration` (if env allows); `just lint`; mypy over changed files
- [x] 4.4 Verify wheel build with new extra: `just build-pack` (or equivalent) succeeds; `packages/onetool-pack/pyproject.toml` extra resolves
- [x] 4.5 Confirm log span continuity: spans `mem.embedding`, `kb.embedding`, `kb.embedding.batch` and events `mem.embedding.retry`/`kb.embedding.retry` appear with the same names as before (unit assertion or manual log check)
