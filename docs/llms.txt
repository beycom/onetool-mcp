# OneTool

> MCP server exposing a single `run` tool for LLM code execution.

OneTool reduces tool-definition context overhead by exposing tools as Python APIs.

## Quick Start

```bash
uv tool install 'onetool-mcp[all]'
onetool init --config ~/.onetool
onetool serve --config ~/.onetool/onetool.yaml --secrets ~/.onetool/secrets.yaml
```

## Invocation

```python
__onetool brave.search(query="AI news")
```

## Packs

Source of truth: `src/ot/config/global_templates/prompts.yaml` (`prompts.packs`).

<!-- BEGIN GENERATED:PACK_SUMMARY -->
| Pack | Extra | Description |
|---|---|---|
| `ot` | `core` | Discover, inspect, operate, and troubleshoot OneTool |
| `arch` | `[dev]` | Validate and generate architecture models, round trips, and bundles |
| `ot_context` | `core` | Store and query large structured results without filling context |
| `ot_forge` | `core` | Create and statically validate OneTool extension packs |
| `ot_image` | `core` | Load, inspect, compare, and manage images through durable handles |
| `ot_llm` | `core` | Transform text or files with a configured language model |
| `ot_secrets` | `core` | Initialize, audit, store, retrieve, rotate, and remove secrets |
| `ot_servers` | `core` | Inspect and control configured outbound MCP proxy servers |
| `ot_timer` | `core` | Measure named operation spans and inspect recorded timings |
| `brave` | `[util]` | Search the web, news, images, and videos with batch support |
| `convert` | `[util]` | Convert PDF and office documents into useful text formats |
| `excel` | `[util]` | Inspect and mutate Excel workbook content and structure |
| `file` | `[util]` | Resolve, read, search, edit, move, copy, and delete files |
| `ground` | `[util]` | Research web, developer, documentation, and Reddit sources with citations |
| `knowledge` | `[util]` | Build and query portable hybrid-search knowledge bases |
| `mem` | `[util]` | Maintain persistent topic memory with search, history, and recovery |
| `tavily` | `[util]` | Search, extract, and run deeper web research with Tavily |
| `chrome_util` | `[dev]` | Annotate and highlight pages through a Chrome DevTools MCP proxy |
| `console` | `core` | Publish inline artifacts to a connected beta OneTool Console app |
| `context7` | `[dev]` | Retrieve current documentation for named software libraries |
| `db` | `[dev]` | Inspect schemas, sample tables, and run explicit database queries |
| `diagram` | `[dev]` | Select providers and render validated diagram source |
| `localhist` | `[dev]` | Save, inspect, restore, and prune project-local snapshot history |
| `package` | `[dev]` | Inspect manifest and registry versions plus available AI models |
| `play_util` | `[dev]` | Annotate and highlight pages through a Playwright MCP proxy |
| `ripgrep` | `[dev]` | Search code and text with regular expressions, globs, and context |
| `whiteboard` | `[util]` | Create and operate live Excalidraw whiteboards with an additive DSL |
| `webfetch` | `[dev]` | Fetch URLs and extract bounded readable content |
<!-- END GENERATED:PACK_SUMMARY -->

## Documentation

- Learn: `docs/learn/`
- Extending: `docs/learn/extension-tools.md`
- Tool Reference: `docs/reference/tools/`
- CLI Reference: `docs/reference/cli/`

## Adding Tools

Create a new extension module (example path):

```python
# src/mytool.py
from ot.decorators import tool

pack = "mytool"

@tool
def hello(*, name: str) -> str:
    """Return a greeting."""
    return f"Hello, {name}!"
```

Then include it in your config `tools_dir` glob.

## Repository

https://github.com/beycom/onetool-mcp

## License

GPLv3
