name: onetool-mcp
version: 1
description: Project-local OneTool skills.

targets:
  - claude-code
  - codex

dependencies:
  apm:
    - git: ./skills
      skills:
        - ot-ref

x-oneskill:
  scope: project
  method: copy
  missingSkillPolicy: fail
