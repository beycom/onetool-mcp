interface:
  display_name: "Use Worker"
  short_description: "Coordinate work through fresh worker episodes"
  default_prompt: "Use $use-worker to coordinate this task through fresh worker episodes."
policy:
  allow_implicit_invocation: false
